<nav class="bg-gray-800 p-4 flex justify-between items-center text-white">
  <div>
    <%= link_to "File Sharing App", root_path, class: "text-xl font-bold" %>
  </div>
  <div class="flex gap-4">
    <% if user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "bg-blue-500 px-4 py-2 rounded" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, class: "bg-blue-500 px-4 py-2 rounded" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "bg-green-500 px-4 py-2 rounded" %>
    <% end %>
  </div>
</nav>
